{
  "name": "ticket-management-system",
  "private": true,
  "scripts": {
    "dev": "bunx concurrently -n server,client -c \"blue.bold,green.bold\" \"bun --filter server dev\" \"bun --filter client dev\"",
    "dev:server": "bun --filter server dev",
    "dev:client": "bun --filter client dev",
    "build": "bun --filter server build && bun --filter client build",
    "typecheck": "bun --filter server typecheck && bun --filter client typecheck"
  },
  "workspaces": ["client", "server"]
}
